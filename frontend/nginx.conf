server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # SPA routing - all routes fallback to index.html
    # Without this, refreshing on /unit/1 would give 404
    location / {
        try_files $uri $uri/ /index.html;
    }
}
